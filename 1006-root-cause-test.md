# 🔍 1006根本原因系统化测试

## 🎯 测试目标
找出导致1006错误的具体触发条件

## 📋 当前基线状态
- ✅ Gateway运行正常 (无认证)
- ✅ 无环境变量污染
- ✅ 最简配置 (无插件)
- ✅ 模型: MiniMax M2.7

## 🧪 测试方案

### 阶段1: 基础稳定性测试 (10分钟)
**目标**: 确认干净基线是否稳定
**方法**: 持续监控，模拟用户交互
**预期**: 应该没有1006

### 阶段2: 逐步添加可能触发因素
按风险从低到高测试：

#### 测试A: 添加token认证
- 风险: 低
- 操作: 添加token认证配置
- 监控: 是否触发1006

#### 测试B: 恢复DeepSeek模型
- 风险: 中
- 操作: 切换回DeepSeek配置
- 监控: 模型切换是否稳定

#### 测试C: 添加环境变量
- 风险: 高 (怀疑是根本原因)
- 操作: 逐步添加嵌入环境变量
- 监控: 哪个变量触发崩溃

#### 测试D: 启用插件
- 风险: 高
- 操作: 逐个启用插件
- 监控: 哪个插件导致问题

### 阶段3: 崩溃复现和分析
- 记录精确的崩溃条件
- 分析崩溃日志
- 制定修复方案

## 🚀 立即开始测试

### 测试1: 基线稳定性 (5分钟)
监控Gateway是否在干净状态下稳定运行

### 测试2: 添加token认证
如果基线稳定，添加认证测试连接稳定性

### 测试3: 环境变量测试
如果前两者稳定，测试环境变量影响

## 📊 测试记录表

| 测试阶段 | 配置变化 | 开始时间 | 持续时间 | 1006出现? | 结论 |
|----------|----------|----------|----------|-----------|------|
| 基线测试 | 无认证, MiniMax | 06:42 | 5分钟 | 待观察 | 待记录 |
| Token测试 | 添加token认证 | 待进行 | 3分钟 | 待观察 | 待记录 |
| 模型测试 | 切换DeepSeek | 待进行 | 3分钟 | 待观察 | 待记录 |
| 环境变量 | 添加嵌入变量 | 待进行 | 2分钟 | 待观察 | 待记录 |

## 🔧 监控工具

### 健康检查监控
```bash
# 每30秒检查一次，持续5分钟
for i in {1..10}; do
    curl -s http://127.0.0.1:18789/health && echo " [OK]" || echo " [FAIL]"
    sleep 30
done
```

### 进程状态监控
```bash
# 监控Gateway进程状态
watch -n 5 "ps aux | grep openclaw-gateway | grep -v grep"
```

### 日志实时监控
```bash
# 监控Gateway日志
tail -f /tmp/openclaw/openclaw-2026-04-16.log 2>/dev/null | grep -i "error\|fail\|1006\|disconnect"
```

## ⚠️ 安全措施

### 回滚机制
每个测试阶段后都可以回滚到基线：
```bash
# 回滚到基线配置
cp /home/boz/.openclaw/openclaw.json.baseline /home/boz/.openclaw/openclaw.json
pkill -f openclaw-gateway
openclaw gateway start
```

### 备份策略
```bash
# 每个测试阶段前备份配置
cp /home/boz/.openclaw/openclaw.json /home/boz/.openclaw/openclaw.json.backup.test-$STAGE
```

## 🎯 成功标准

### 测试成功
- ✅ 无1006错误
- ✅ Gateway持续运行
- ✅ Control UI稳定连接

### 测试失败 (但有用)
- ❌ 出现1006错误
- ✅ 记录了触发条件
- ✅ 可以回滚到稳定状态

## 📈 预期成果

### 最佳情况
找到1006的具体触发条件，避免未来发生

### 最差情况
确认干净基线稳定，但某些配置会触发问题

### 中间情况
部分配置安全，部分配置危险，建立安全边界

## 🚨 紧急停止
如果测试导致系统完全崩溃：
```bash
# 完全重置
pkill -9 -f openclaw
rm -rf /home/boz/.openclaw
openclaw init --force
```

## 🍎 测试哲学
> **"每个崩溃都是一次学习机会，每个1006都隐藏着根本原因"**

**当前状态**: 准备开始基线测试
**下一步**: 监控5分钟基线稳定性